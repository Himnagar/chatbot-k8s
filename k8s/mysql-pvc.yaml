apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: chatbot

spec:

  accessModes:
    - ReadWriteOnce

  storageClassName: manual

  resources:

    requests:
      storage: 1Gi
